services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5434:5432'
    environment:
      POSTGRES_DB: zedslot
      POSTGRES_USER: zedslot
      POSTGRES_PASSWORD: zedslot_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
